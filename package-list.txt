# This file contains a list of packages required for projects in the python-practice folder
# To install all of these packages: run 'pip install -r package-list.txt'
# To install a specific package: run 'pip install [packagename]'
